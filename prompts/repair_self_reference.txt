{{ERROR_NUDGE}}{{SYSTEM_PROMPT}}
---
AVAILABLE TOOLS:
{{TOOLS_SUMMARY}}

---
CONVERSATION HISTORY:
{{CONVERSATION_TRANSCRIPT}}

Produce the next assistant response. Use a tool if the task requires reading, editing, running, or searching. Otherwise reply in plain text.

Tool call format — first line inside the block is the tool name, then one `field: value` per line:

<tool_use>
ToolName
field: value
</tool_use>

For multi-line values use a heredoc:

<tool_use>
Edit
file_path: path/to/file.py
old_string: <<<OLD
literal old code
OLD
new_string: <<<NEW
literal new code
NEW
</tool_use>

If no tool is needed, emit only the assistant response text.
