The following request was originally intended for Claude Code. Produce the next assistant response while respecting all instructions, tools, and context contained in the request.

{{ERROR_NUDGE}}Preserve the user's objective exactly. Choose the minimum action needed to satisfy the user's request. Do not perform verification, installation, builds, testing, editing, or implementation unless the user's request requires it.

Before calling a tool, ask: "Is this tool necessary to answer the user's request?" If the answer is no, respond to the user instead of calling a tool.

If the next assistant response requires tool use, emit each tool call inside a <tool_use> block using the delimiter format below. DO NOT emit JSON, do not escape anything, do not wrap values in quotes.

FORMAT — first line is the tool name, then one `field: value` per line:

<tool_use>
ToolName
field_one: short inline value
field_two: another inline value
</tool_use>

For any value that contains newlines, code, quotes, backslashes, or Windows paths, use a heredoc with a unique tag:

<tool_use>
Edit
file_path: path/to/file.py
old_string: <<<OLD
def hello():
    print("hi")
OLD
new_string: <<<NEW
def hello():
    print("hello, world")
NEW
</tool_use>

Rules:
- Heredoc opener is `field: <<<TAG`. The closing line must be exactly `TAG` on its own line, with no surrounding whitespace.
- Pick a TAG that does not appear inside the value (OLD, NEW, BODY, PATCH, EOF, etc).
- Do not JSON-escape inside heredocs. Write literal newlines, literal backslashes, literal quotes — exactly as they should appear in the file.
- Inline values (no heredoc) must fit on a single line and must not contain a literal newline.
- For array or object fields (such as MultiEdit `edits`, TodoWrite `todos`, Bash `command_lines`), put compact JSON inside a heredoc:

  edits: <<<EDITS
  [{"old_string":"foo","new_string":"bar"}]
  EDITS

- Boolean/number fields can be written as inline values like `replace_all: false` or `offset: 0`.
- Emit only one tool name per <tool_use> block. Emit multiple <tool_use> blocks if multiple tool calls are needed.

Otherwise (no tool needed) emit only the assistant response content.

<claude_code_request_json>
{{REQUEST_JSON}}
</claude_code_request_json>
